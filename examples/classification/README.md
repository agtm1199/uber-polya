# Customer Churn Classification

Predict which customers will churn (cancel their subscription) based on usage and account features, using three competing classifiers.

## Problem

- **Data**: ~1,000 synthetic customers with 6 features (seed=42)
- **Features**: tenure (months), monthly charges, total charges, support tickets, contract type (month-to-month vs annual), internet service (yes/no)
- **Target**: churned (1) vs retained (0), ~30% churn rate
- **Churn drivers**: shorter tenure, more support tickets, and month-to-month contracts increase churn probability
- **Question**: Which model best predicts churn, and which features matter most?

## Files

| File | Purpose |
|------|---------|
| `classification_solver.py` | Solver: Logistic Regression + Random Forest + Gradient Boosting, cross-validation, verification |
| `solution.json` | Machine-readable solution (generated by solver) |

## Requirements

```bash
pip install numpy pandas scikit-learn
```

## Quick Run

```bash
python3 classification_solver.py
```

## Expected Output

- **Best model**: Logistic Regression (accuracy ~80%, ROC-AUC ~0.85)
- **All models beat majority-class baseline** (~70% accuracy by always predicting "not churned")
- **Top features** (Random Forest importance): tenure, total charges, monthly charges, support tickets
- **Cross-validation**: stable accuracy (std < 0.05 across 5 folds)
- **All 7 verification checks pass**

## Algorithm

1. **Logistic Regression**: Linear model with L2 regularization. Fast baseline. Provides interpretable coefficients for each feature.

2. **Random Forest** (100 trees): Bagged ensemble of decision trees. Handles non-linear relationships and feature interactions. Produces feature importance rankings.

3. **Gradient Boosting** (100 trees): Sequential boosting of shallow trees. Often achieves the best accuracy on tabular data. Minimizes log-loss via gradient descent in function space.

## Key Concepts

- **Accuracy**: fraction of correct predictions (TP + TN) / total
- **Precision**: of those predicted as churned, how many actually churned (TP / (TP + FP))
- **Recall**: of those who actually churned, how many were caught (TP / (TP + FN))
- **F1 score**: harmonic mean of precision and recall
- **ROC-AUC**: area under the receiver operating characteristic curve; measures discrimination across all thresholds
- **Confusion matrix**: 2x2 table of TP, FP, FN, TN
- **Feature importance**: mean decrease in impurity (Gini) across all trees in the Random Forest
- **Cross-validation**: 5-fold stratified CV estimates out-of-sample performance and its variability

## Domain

Machine Learning -- Supervised Classification, Ensemble Methods, Model Comparison.
