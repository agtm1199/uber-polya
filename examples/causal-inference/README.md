# Causal Inference -- Job Training Program

A company offered a voluntary job training program. Participation was not random -- employees with higher education and more experience were more likely to enroll. We want to estimate the causal effect of training on salary, correcting for selection bias.

## Problem

- **Treated**: 500 employees who participated in the training program (approximately)
- **Control**: 500 employees who did not participate (approximately)
- **Covariates**: education_years, experience_years, age (all affect both participation and salary)
- **Confounding**: Higher-education, more-experienced employees self-select into training AND earn more
- **Question**: What is the causal effect of the training program on salary?
- **Ground truth**: The data is synthetic with a true treatment effect of $5,000

## Files

| File | Purpose |
|------|---------|
| `causal_solver.py` | Solver: naive estimate, propensity score matching, difference-in-differences, doubly robust estimation |
| `solution.json` | Machine-readable solution (generated by solver) |

## Requirements

```bash
pip install numpy scipy scikit-learn statsmodels
```

## Quick Run

```bash
python causal_solver.py          # solve + verify
```

## Expected Output

- **Naive difference**: Overestimates the effect (well above $5,000) due to selection bias
- **Propensity Score Matching (ATT)**: Recovers approximately $5,000
- **Difference-in-Differences**: Recovers approximately $5,000
- **Doubly Robust (AIPW)**: Recovers approximately $5,000
- **Verification**: All 8 checks pass -- naive is biased, causal methods are closer to truth

## Key Concepts

- **Selection bias**: Treated and control groups differ systematically on covariates, inflating naive estimates
- **Propensity score**: P(treatment | covariates) -- summarizes confounding into a single number for matching
- **ATE vs ATT**: Average Treatment Effect (population) vs Average Treatment Effect on the Treated (those who participated)
- **Confounding**: When a variable affects both treatment assignment and outcome
- **Doubly robust estimation**: Combines propensity model with outcome model; consistent if either model is correct

## Domain

Causal Inference -- Propensity Score Matching, Difference-in-Differences, Doubly Robust Estimation.
