# Customer Survival -- SaaS Subscription Churn

Analyze SaaS customer churn patterns using survival analysis: Kaplan-Meier curves, log-rank test, and Cox Proportional Hazards regression.

## Problem

- **Customers**: 500 SaaS subscribers with subscription duration (months) and churn status
- **Censoring**: ~30% of customers are right-censored (still active, have not churned)
- **Plan types**: Basic (n=300, median survival ~12 months) and Pro (n=200, median survival ~24 months)
- **Covariate**: Support ticket count (higher tickets correlate with higher churn risk)
- **Questions**: Do Basic and Pro plans have different churn rates? What factors predict churn?

## Files

| File | Purpose |
|------|---------|
| `survival_solver.py` | Solver: Kaplan-Meier + log-rank test + Cox PH regression |
| `solution.json` | Machine-readable solution (generated by solver) |

## Requirements

```bash
pip install lifelines numpy pandas scipy
```

## Quick Run

```bash
python survival_solver.py          # solve + verify
```

## Expected Output

- **Kaplan-Meier**: Monotonically decreasing survival curves for Basic and Pro plans
- **Median survival**: Basic ~12 months, Pro ~24 months
- **Log-rank test**: p < 0.05 (significant difference between plans)
- **Cox PH hazard ratios**: Basic vs Pro HR > 1.0 (Basic has higher hazard), tickets HR > 1.0 (more tickets = higher churn)
- **95% CI**: Hazard ratio confidence intervals excluding 1.0 for both covariates
- **Concordance index**: > 0.55 (better than random prediction)
- **PH assumption**: Schoenfeld residuals test for proportional hazards

## Algorithm

1. **Kaplan-Meier estimator**: Non-parametric survival function estimate per plan group, handling right-censored observations
2. **Log-rank test**: Non-parametric hypothesis test comparing survival distributions between Basic and Pro plans
3. **Cox Proportional Hazards**: Semi-parametric regression modeling hazard as a function of plan type and support ticket count; reports hazard ratios with 95% confidence intervals
4. **Diagnostics**: Proportional hazards assumption check via Schoenfeld residuals; concordance index for predictive accuracy

## Key Concepts

- **Survival function S(t)**: Probability of surviving (not churning) beyond time t
- **Hazard rate h(t)**: Instantaneous churn risk at time t, given survival to t
- **Right censoring**: Customers still active at observation end -- we know they survived at least that long, but not when (or if) they will churn
- **Kaplan-Meier estimator**: Non-parametric estimate of S(t) that handles censored data via the product-limit formula
- **Log-rank test**: Compares survival curves between groups by comparing observed vs expected events at each time point
- **Cox Proportional Hazards**: Models h(t) = h0(t) * exp(beta * X); the hazard ratio exp(beta) quantifies how each covariate multiplicatively shifts risk
- **Hazard ratio**: HR > 1 means higher risk; HR < 1 means lower risk; HR = 1 means no effect

## Domain

Statistical Inference -- Survival Analysis, Kaplan-Meier Estimation, Log-Rank Test, Cox Proportional Hazards, Censored Data.
